2. Метод Ньютона
Дан код функции `sqrt(...)`: https://github.com/IgorWolkov/workshops/blob/master/basics/src/main/scala/week1/session.sc
Объясните, почему функция `isGoodEnough(...)` плохо работает для малых чисел и уходит в бесконечный цикл на больших
числах.
Измените функцию `isGoodEnough(...)` так, чтобы она хорошо работала на малых и больших числах.
Проверьте функцию на числах: 0.001, 0.1e-20, 1.0e20, 1.0e50.

Notes:
Попросить Игоря расказать об этой задаче подробно(((